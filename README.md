# fs6090_yolov4
#omz_projects

1. Install requirements
2. Add yolo darknet weights, config file and classes files
3. Rename the weights file to yolov3, config file = config and classes file = custom
4. import the video and rename to input
